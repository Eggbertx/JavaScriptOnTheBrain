---
import Banner from './Banner.astro';
import TabBar from './TabBar.astro';
const { siteTitle, activePage, activeTab } = Astro.props;
---

<header>
	<Banner siteTitle={siteTitle} />
	<TabBar activePage={activePage} activeTab={activeTab} />
</header>